import Funcionario from '../../@types/models/funcionario'
const initialState: Funcionario[] = []
export default function funcionarioReducer(state: any = initialState, action: any) {
  return state;
}
